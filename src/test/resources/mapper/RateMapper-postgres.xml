<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enixyu.djolar.mybatis.mapper.RateMapper">
  <select id="find" resultType="com.enixyu.djolar.mybatis.domain.Rate">
    SELECT "rate"."id", "rate"."level" FROM "rate"
  </select>
</mapper>